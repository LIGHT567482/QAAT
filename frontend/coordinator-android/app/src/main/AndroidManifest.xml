<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet only for the once-a-day manifest pull + post-session sync. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- LocalOnlyHotspot + reading its config (the room Wi-Fi). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- requestNetwork(), which is how a STUDENT phone takes and gives back a turn on the class
         the geofence a register is opened with. -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Keep the in-room server + session alive while screen is off. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".App"
        android:label="KIU QAAT"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.QAATCoordinator"
        android:usesCleartextTraffic="true">  <!-- the embedded server serves http:// on the LAN -->

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
